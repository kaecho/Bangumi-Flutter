import 'package:go_router/go_router.dart';

/// 条目域路由 (条目详情及其子页面)
/// 由 subject 模块的屏幕填充
final List<GoRoute> subjectRoutes = [
  // GoRoute(path: '/subject/:id', builder: (_, _) => const SubjectScreen()),
  // GoRoute(path: '/subject/:id/info', builder: (_, _) => const SubjectInfoScreen()),
  // GoRoute(path: '/subject/:id/episodes', builder: (_, _) => const EpisodesScreen()),
  // GoRoute(path: '/subject/:id/characters', builder: (_, _) => const CharactersScreen()),
  // GoRoute(path: '/subject/:id/persons', builder: (_, _) => const PersonsScreen()),
  // GoRoute(path: '/subject/:id/rating', builder: (_, _) => const RatingScreen()),
  // GoRoute(path: '/subject/:id/preview', builder: (_, _) => const PreviewScreen()),
  // GoRoute(path: '/subject/:id/tag', builder: (_, _) => const SubjectTagScreen()),
  // GoRoute(path: '/subject/:id/typerank', builder: (_, _) => const SubjectTypeRankScreen()),
  // GoRoute(path: '/subject/:id/works', builder: (_, _) => const WorksScreen()),
  // GoRoute(path: '/subject/:id/relations', builder: (_, _) => const RelationsScreen()),
  // GoRoute(path: '/subject/:id/voices', builder: (_, _) => const VoicesScreen()),
  // GoRoute(path: '/mono/:type/:id', builder: (_, _) => const MonoScreen()),
  // GoRoute(path: '/mono/:type/:id/subjects', builder: (_, _) => const MonoSubjectsScreen()),
];
